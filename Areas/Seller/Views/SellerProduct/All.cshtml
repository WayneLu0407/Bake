@model IEnumerable<Bake.Areas.Seller.ViewModels.ProductListViewModel>
<!-- ========== section start ========== -->
<section>
    <div class="container-fluid">
        <!-- ========== title-wrapper start ========== -->
        <div class="title-wrapper pt-30">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <div class="title d-flex align-items-center flex-wrap">
                        <h2 class="mr-40">所有商品</h2>
                    </div>
                </div>
            </div>
            <!-- end row -->
        </div>
        <!-- ========== title-wrapper end ========== -->
        <!-- ======== card style start ========== -->
        <div class="card-style px-0 mb-30">
            <div class="title d-flex flex-wrap pl-30 pr-30 mb-10 align-items-center justify-content-between">
                <div class="d-flex align-items-center ">
                    <h6>排序:</h6>
                    <div class="select-style-1 mb-0 ml-10">
                        <div class="select-position">
                            <select class="p-0 pe-5 border-0">
                                <option value="">價格</option>
                                <option value="">品名</option>
                                <option value="">過期日</option>
                            </select>
                        </div>
                    </div>
                </div>
                <p>
                    <a asp-action="New" class="main-btn primary-btn btn-hover btn-sm"><i class="lni lni-plus mr-5"></i>新增</a>
                </p>
            </div>
            <div class="table-wrapper table-responsive task-table">
                <table class="table text-center">
                    <thead>
                        <tr>
                            <th class="min-width">商品圖片</th>
                            <th class="min-width">商品名稱</th>
                            <th class="min-width">商品分類</th>
                            <th class="min-width d-none d-md-table-cell" style="width:15%">商品描述</th>
                            <th class="min-width" style="width:8%">商品價格</th>
                            <th class="min-width d-none d-md-table-cell" style="width:8%">優惠折數</th>
                            <th class="min-width" style="width:8%">商品數量</th>
                            <th class="min-width d-none d-md-table-cell">商品過期日</th>
                            <th class="min-width">編輯</th>
                            <th class="min-width"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="min-width">
                                    <div>
                                        <img src="@item.ProductImage" class="img-fluid rounded"
                                             style="width:120px; height:90px; object-fit:cover;" alt="" />
                                    </div>
                                </td>
                                <td class="min-width">
                                    <p>
                                        @item.ProductName
                                    </p>
                                </td>
                                <td class="min-width ">
                                    <p>
                                        @item.CategoryName
                                    </p>
                                </td>
                                <td class="min-width d-none d-md-table-cell">
                                    <p>
                                        @item.ProductDescription
                                    </p>
                                </td>
                                <td class="min-width">
                                    <p>
                                        @item.ProductPrice
                                    </p>
                                </td>
                                <td class="min-width d-none d-md-table-cell">
                                    <p>
                                        @item.ProductDiscount
                                    </p>
                                </td>
                                <td class="min-width ">
                                    <p> @item.ProductQuantity</p>
                                </td>
                                <td class="min-width d-none d-md-table-cell">
                                    <p>
                                        @item.ExpireDate
                                    </p>
                                </td>
                                
                                <td class="min-width">
                                    <div class="action justify-content-end">
                                        <button class="text-dark link-btn">
                                            <a asp-area="Seller" asp-controller="SellerProduct" asp-action="Edit"
                                               asp-route-id="@item.ProductId"></a>
                                            <i class="lni lni-pencil"></i>
                                        </button>
                                        <!-- 刪除按鈕，必須用 form POST -->
                                        <form asp-area="Seller" asp-controller="SellerProduct"
                                              asp-action="Delete" asp-route-id="@item.ProductId"
                                              method="post" style="display:inline"
                                              onsubmit="return confirm('確定要刪除【@item.ProductName】？')">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="text-dark delete-btn ml-10">
                                                <i class="lni lni-trash-can"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <!-- end table -->
            </div>
        </div>
        <!-- ======== card style end ========== -->

    </div>
    <!-- end container -->
</section>
<!-- ========== section end ========== -->
